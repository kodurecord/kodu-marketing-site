Reserved for static assets (e.g. an Open Graph preview image at assets/og.png,
or a favicon). The page renders fully without anything here — all evidence
imagery is generated inline as SVG. Drop the marketing site's own brand assets
in as needed.
